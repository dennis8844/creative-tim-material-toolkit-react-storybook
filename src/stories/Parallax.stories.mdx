import { Meta, Canvas, Story } from '@storybook/addon-docs/blocks';
import Parallax from '../_material-toolkit-components/Parallax/Parallax';
import Background2 from '../_material-toolkit-components/_assets/img/bg2.jpg';
import GridContainer from '../_material-toolkit-components/Grid/GridContainer';
import GridItem from '../_material-toolkit-components/Grid/GridItem';
import Card from "../_material-toolkit-components/Card/Card.js";
import Button from "../_material-toolkit-components/CustomButtons/Button.js";

<Meta title="Components/Parallax" component={Parallax} />

# Parallax

## Styles

You will find all the styles for these components in
`../_assets/jss/material-kit-react/components/parallaxStyle.js`.

## Usage

If you want a nice parallax like that in our demo pages, you can add the following code at the beginning of your page.

```javascript
import Parallax from "components/Parallax/Parallax.js";
<Parallax filter image={require("path/to/your/image")} />
```

## Example

<Canvas>
  <Story name="Parallax">
    <Parallax  filter image={Background2}>
      <Button color="rose" round>
        Scroll Me
      </Button>
    </Parallax>
    <GridContainer>
      <GridItem xs={12} sm={12} md={8}>
        <Card>
          I'm a Grid Item on a Card
          <Button color="success" round>
              Scroll Me
          </Button>
        </Card>
      </GridItem>
    </GridContainer>
  </Story>
</Canvas>

# Props

```javascript
Parallax.propTypes = {
  className: PropTypes.string,
  filter: PropTypes.bool,
  children: PropTypes.node,
  style: PropTypes.string,
  image: PropTypes.string
};
```



